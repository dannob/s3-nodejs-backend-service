
{
  "name": "s3-backend-service",
  "version": "1.0.0",
  "description": "Backend service for CRUD operations on S3 using AWS SDK for JavaScript v3",
  "main": "src/index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "supertest": "^6.1.3",
    "aws-sdk-client-mock": "^1.0.0"
  }
}
